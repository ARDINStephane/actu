<div class="container mt-5">
    {{ form_start(form, {'action': path('serie.search'), 'method': 'GET'}) }}
    <div class="form-row row justify-content-md-center">
        <div class="col-2 mt-1">
            {{ form_row(form.search) }}
        </div>
        <div class="col-2">
            <button class="btn btn-primary">{{ 'search.search' | trans }}</button>
        </div>
    </div>
    {{ form_end(form) }}
</div>
